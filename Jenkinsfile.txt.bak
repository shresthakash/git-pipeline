pipeline {
    agent any
    parameters {
        string(name: 'Version', defaultValue: 'Mr Jenkins', description: 'Who should I say hello to?')
		}
		
		stages { 
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}